// #pragma once is another way to create header guard
// Warning: Its a new feature, so not all compilers support it
// Also, the implementation of #pragma directives are different for
// different compilers. Not all #pragma commands run for all compilers
#pragma once

int getSquareSides() {
    return 4;
}